# Uncomment the next line to define a global platform for your project
platform :ios, '11.4'

target 'ios-study-firebase-remote-config' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-study-firebase-remote-config
  pod 'Firebase/RemoteConfig'

  target 'ios-study-firebase-remote-configTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-study-firebase-remote-configUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
